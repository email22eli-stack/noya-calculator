# מחשבון לבגרות מתמטיקה 5 יחידות

## על הפרויקט
אפליקציית Python עם GUI (tkinter) המיועדת לתלמידי בגרות מתמטיקה 5 יחידות בישראל.
הפרויקט התחיל כ-"Hello World" ללמידת שיתוף פעולה עם Claude Code.

## קובץ ראשי
- `calculator.py` — כל הקוד נמצא בקובץ אחד

## להרצה
```
py calculator.py
```

## תלויות (כבר מותקנות)
```
py -m pip install sympy matplotlib pyinstaller
```

## תכונות קיימות
1. **מחשבון רגיל** — חיבור, חיסור, כפל, חילוק, אחוז, החלפת סימן
2. **פותר משוואה ריבועית** (כפתור `x²`) — ax²+bx+c=0, כולל פתרונות מרוכבים
3. **דף נוסחאות** (כפתור `נוסחאות`) — 7 נושאים, חיפוש חי, העתקה ללוח
   - אלגברה, סדרות, טריגונומטריה, גיאומטריה אנליטית, חדו"א, וקטורים, מספרים מרוכבים
4. **חקר פונקציה** (כפתור `חקר f(x)`) — דוח מלא לפי דרישות הבגרות:
   - תחום הגדרה, חיתוכים עם הצירים, אסימפטוטות
   - נגזרת ראשונה + נקודות קיצון + תחומי עלייה/ירידה
   - נגזרת שנייה + נקודות פיתול + תחומי קעירות/קמירות
   - גרף אינטראקטיבי עם סימון נקודות מיוחדות (matplotlib)

## עיצוב
- ערכת צבעים: Catppuccin Mocha (כהה)
- צבעי כפתורים: כחול=אופרטורים, ירוד=x², כתום=נוסחאות, סגול=חקר f(x), אדום/ורוד==

## שלבים הבאים אפשריים
- אפשרות ב' בדף הנוסחאות: כפתורי קיצור עם פונקציות נפוצות לחקר
- בניית exe עם PyInstaller לשיתוף ללא התקנות
- הוספת אינטגרל מוגדר
- שיפור קלט — תמיכה בסינטקס טבעי יותר (למשל x^2 במקום x**2)
